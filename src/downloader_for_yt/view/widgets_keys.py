import enum


class WidgetKeys(enum.Enum):
    format: str = "format"
    browser: str = "browser"
    video_format: str = "video_format"
    audio_format: str = "audio_format"
    mixed_format: str = "mixed_format"
    url: str = "url"
    show_format: str = "show_format"
    submit: str = "submit"
    load_video: str = "load_video"

